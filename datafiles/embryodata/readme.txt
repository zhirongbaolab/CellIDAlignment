Position and Manual Names, both the subset used to create initial manual alignment (names specified in script) and those used to assess error in result.

e1,e2 alignment driver scripts expect these to be loaded in the workspace (along with the florescence derived file)
e9 reads the files from xls files in an attempt to minimize manual mishaps. 